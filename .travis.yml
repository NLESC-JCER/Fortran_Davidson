sudo: require
language: generic

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - cmake
      - gfortran
      - libblas-dev
      - liblapack-dev

script:
  - cmake -H. -Bbuild && cmake --build build
  - ./bin/main
  
branches:
  only:
   - master

